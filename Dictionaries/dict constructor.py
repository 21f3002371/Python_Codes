car = {'car_name' : "a", 'car_no' :"b"}
print(car)
print(car.get('car_name'))
print(car.keys()) #Returns list of dict keys
print(car.values()) #Returns list of dict values
# Items is a key-value pair
print(car.items()) # Returns list of key-value pairs
